# Delivery

## Purpose

This directory holds active governed work packages produced by the `CTO Desk`
workflow.

Use it for:
- active rollout targets
- cross-phase execution records
- durable delivery artifacts that should stay grouped together

## Rule

Standards live in `governance/standards/`.
Reusable templates live in `templates/`.
Active execution packets live here.
